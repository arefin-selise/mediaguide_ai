name: mediaguide_ai
description: MediGuide AI – Personal AI Health Coach (skeleton app)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  image_picker: ^1.1.2
  fl_chart: ^0.69.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
